package events.eventutils;

/**
 * Enum to hold the type of the Event. If a new type is added,
 * the scores particular to that event has to be added to 
 * {@link Constants} class too.
 *
 */
public enum EventType {
	GOLD,
	SILVER,
	BRONZE
}
